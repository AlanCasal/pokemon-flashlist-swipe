import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
	gradient: {
		position: 'absolute',
		left: 0,
		right: 0,
		height: '25%',
	},
	topGradient: {
		top: 0,
	},
	bottomGradient: {
		bottom: 0,
	},
});

export default styles;
