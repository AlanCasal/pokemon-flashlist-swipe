import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
	container: {
		alignItems: 'center',
		paddingVertical: 10,
	},
	indicator: {
		borderRadius: 3,
		height: 6,
		overflow: 'hidden',
		width: 80,
	},
});

export default styles;
